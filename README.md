# solana-tools-lite

🛠 A lightweight, modular CLI toolkit for working with Solana keypairs and signatures — with `--explain` mode and CI-friendly JSON output.

## Commands

- `gen` — Generate keypair from mnemonic
- `sign` — Sign message
- `verify` — Verify signature
- `base58` — Encode/decode data
- `--json`, `--explain` — Optional flags

## Why?

Existing tools like `solana-cli` are powerful but monolithic. This tool is lightweight, educational, and CI-friendly 🖤
